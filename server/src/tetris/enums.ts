/**
 * This enum is outside share types because i had a problem with jest: https://github.com/kulshekhar/ts-jest/issues/281
 */
export const enum PlayerInput {
  NONE = "",
  LEFT = "LEFT",
  RIGHT = "RIGHT",
}